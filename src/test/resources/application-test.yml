spring:
  config:
    activate:
      on-profile: test

  datasource:
    main:
      hikari:
        driver-class-name: org.postgresql.Driver
        username: test
        password: test
    standby:
      hikari:
        driver-class-name: org.postgresql.Driver
        username: test
        password: test

jwt:
  secret: ${SECRET}
  token-validity-time: ${TOKEN_VALIDITY_TIME}
